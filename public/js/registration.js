(function($){
	/*function validarExistenciaEmail(email){
		//Consultar un recurso web para exttraer todos los emails registrados
		//llenar las opciones de carreras
		var estado = false;
		$.ajax({
			url:"/rrhh/api/users/validate_email/retrieve.json?u_email=" + email,
		})
		.done(function(data) {
			//validar estado
			estado = data.encontrado;
		});
		return estado;
	}*/
}(jQuery));